import { Layout } from 'antd';

const Header = () => {
  const { Header } = Layout;
  return (
    <Layout className="layout">
      <Header>Profile App</Header>
    </Layout>
  )
}

export default Header;